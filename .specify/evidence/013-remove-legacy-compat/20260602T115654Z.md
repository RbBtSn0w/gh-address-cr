# Verification Evidence: 013-remove-legacy-compat

**Archived**: 20260602T115654Z
**Build Status**: PASS

## Spec Verification Checklist

- [x] FR-001: Current supported workflow surface is defined and separated from historical behavior — verified by `python -m gh_address_cr --help`, `python -m gh_address_cr agent manifest`, and `tests/test_runtime_packaging.py`.
- [x] FR-002: Superseded compatibility paths are removed or disabled — verified by deleted obsolete handler modules and `tests/test_runtime_packaging.py::test_installed_runtime_does_not_carry_legacy_command_scripts`.
- [x] FR-003: Unsupported legacy usage is rejected before side effects — verified by `python -m gh_address_cr cr-loop --help`, `session-engine --help`, and `clean-state --help`, each exiting `2`.
- [x] FR-004: Rejections include actionable current workflow guidance — verified by legacy command stderr naming `review`, `address`, and `agent` alternatives.
- [x] FR-005: Active guidance avoids removed compatibility paths — verified by docs/spec updates and plugin payload check.
- [x] FR-006: Historical references are scoped to archival/spec/test assertions — verified by residual scan limited to 013 requirements/tests and older archival specs.
- [x] FR-007: Current supported workflows remain usable — verified by `tests.test_native_runtime_boundary`, `tests.test_python_wrappers`, `tests.test_cr_loop`, `tests.test_batch_github_execute`, and CLI smoke.
- [x] FR-008: Audit evidence for review handling is preserved — verified by full test suite and affected workflow suites covering classification, reply, resolve, and final-gate paths.
- [x] FR-009: No new hidden fallbacks or silent shims were added — verified by runtime subprocess calls using `python -m gh_address_cr.commands.<module>` and removal of handler/script dispatcher constants.
- [x] FR-010: Current workflows complete without removed compatibility behavior — verified by `tests.test_native_runtime_boundary` and full test suite.
- [x] FR-011: Runtime does not retain handler-named packages or obsolete handler modules — verified by `tests.test_runtime_packaging.RuntimePackagingTest.test_current_runtime_commands_are_not_handler_package_named`, `test_runtime_cli_has_no_legacy_or_handler_script_dispatcher`, and installed package inspection.

## Commands

### Targeted RED/GREEN Contract

`/opt/homebrew/bin/pyenv exec python -m unittest tests.test_runtime_packaging.RuntimePackagingTest.test_current_runtime_commands_are_not_handler_package_named tests.test_runtime_packaging.RuntimePackagingTest.test_runtime_cli_has_no_legacy_or_handler_script_dispatcher tests.test_runtime_packaging.RuntimePackagingTest.test_installed_runtime_does_not_carry_legacy_command_scripts`

Result: PASS, 3 tests.

### Affected Suite

`/opt/homebrew/bin/pyenv exec python -m unittest tests.test_runtime_packaging tests.test_native_runtime_boundary tests.test_python_wrappers tests.test_aux_scripts tests.test_cr_loop tests.test_batch_github_execute tests.test_network_write_contracts tests.test_submit_action tests.test_submit_action_helper`

Result: PASS, `Ran 214 tests in 105.119s`, `OK`.

### Lint

`/opt/homebrew/bin/pyenv exec ruff check src tests`

Output: `All checks passed!`

### Full Unit Tests

`/opt/homebrew/bin/pyenv exec python -m unittest discover -s tests`

Result: PASS, `Ran 510 tests in 122.567s`, `OK`.

### CLI Smoke

`/opt/homebrew/bin/pyenv exec python -m gh_address_cr --help`

Result: PASS. Help lists current public commands and does not list removed legacy commands.

### Agent Manifest

`/opt/homebrew/bin/pyenv exec python -m gh_address_cr agent manifest`

Result: PASS. Manifest status: `MANIFEST_READY`; public commands include current workflow commands.

### Plugin Payload

`/opt/homebrew/bin/pyenv exec python scripts/build_plugin_payload.py --check`

Output: `plugin payload is up to date`.

### Diff Check

`git diff --check`

Result: PASS, exit 0.

### Unsupported Legacy Commands

`/opt/homebrew/bin/pyenv exec python -m gh_address_cr cr-loop --help`

Output: `Unsupported legacy command: cr-loop. Use current workflows such as ...`; exit `2`.

`/opt/homebrew/bin/pyenv exec python -m gh_address_cr session-engine --help`

Output: `Unsupported legacy command: session-engine. Use current workflows such as ...`; exit `2`.

`/opt/homebrew/bin/pyenv exec python -m gh_address_cr clean-state --help`

Output: `Unsupported legacy command: clean-state. Use current workflows such as ...`; exit `2`.

### Residual Scan

`rg -n "legacy_handlers|command_handlers|legacy_scripts|SCRIPT_DIR|COMPAT_SCRIPT_DIR|src/gh_address_cr/(legacy_handlers|command_handlers)|gh_address_cr\\.(legacy_handlers|command_handlers)" src tests specs/013-remove-legacy-compat skill README.md pyproject.toml`

Result: PASS for production cleanup. Matches are limited to 013 removal requirements/tests and native-boundary tests that assert legacy names remain absent.
